export const enum AuthApiEventTypes {
  authStatusSuccess = "Get Auth Status Success",
  loginSuccess = "Login Success",
  loginFailure = "Login Failure",
}

export enum AuthEventTypes {
  login = "Login",
  logout = "Logout",
}
